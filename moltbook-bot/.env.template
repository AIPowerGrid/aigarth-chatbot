# Moltbook agent API key (from https://www.moltbook.com after registering)
MOLTBOOK_API_KEY=moltbook_xxx

# AI Power Grid (same as Discord bot)
GRID_API_KEY=your_grid_api_key_here
GRID_MODEL=grid/meta-llama/llama-4-maverick-17b-128e-instruct

# Submolts to subscribe to (comma-separated). Bot reads feed and may comment here.
MOLTBOOK_SUBMOLTS=general,aithoughts

# Submolts where to post promos (default: same as MOLTBOOK_SUBMOLTS)
# MOLTBOOK_AD_SUBMOLTS=general,aithoughts
# Optional: only post promos to these "top" submolts (e.g. general,aithoughts)
# MOLTBOOK_TOP_SUBMOLTS=general,aithoughts

# Optional: extra post IDs to check for comments (e.g. posts you made manually). Posts the bot creates are auto-saved to our_post_ids.txt.
# MOLTBOOK_OUR_POST_IDS=uuid1,uuid2
# Set to 1 to log why comment replies / API response shape (debug)
# MOLTBOOK_DEBUG=1

# Poll interval in seconds (default 300 = 5 min)
# MOLTBOOK_POLL_INTERVAL=300
# MOLTBOOK_FEED_SORT=new
# MOLTBOOK_FEED_LIMIT=15

# ChromaDB path (default from repo root)
# CHROMA_DB_PATH=./chroma_db
